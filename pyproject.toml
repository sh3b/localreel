[project]
name = "localreel"
version = "0.1.0"
description = "Self-hosted short-form video platform"
requires-python = ">=3.14.5"
dependencies = []

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["src", "."]

[dependency-groups]
dev = [
    "factory-boy>=3.3.3",
    "pytest>=9.1.1",
]
